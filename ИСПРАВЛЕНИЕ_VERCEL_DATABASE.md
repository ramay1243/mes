# 🔧 Исправление ошибки базы данных на Vercel

## ❌ Проблема:

Vercel не поддерживает SQLite файлы напрямую - файловая система временная и не постоянная.

## ✅ Решение 1: Использовать Vercel Postgres (РЕКОМЕНДУЮ!)

Vercel Postgres - это встроенная база данных, очень простая в настройке!

### Шаг 1: Измените схему Prisma

В файле `prisma/schema.prisma` измените:

```prisma
datasource db {
  provider = "postgresql"  // Было: sqlite
  url      = env("DATABASE_URL")
}
```

### Шаг 2: Создайте базу данных на Vercel

1. Зайдите на https://vercel.com
2. Откройте ваш проект `messenger`
3. Вкладка **"Storage"**
4. Нажмите **"Create Database"**
5. Выберите **"Postgres"**
6. Нажмите **"Create"**

### Шаг 3: Подключите базу к проекту

1. После создания базы, Vercel автоматически добавит `DATABASE_URL` в Environment Variables
2. Проверьте в **"Settings"** → **"Environment Variables"** - должна быть `DATABASE_URL`

### Шаг 4: Примените миграции

1. В вашем проекте на Vercel откройте **"Deployments"**
2. Найдите последний деплой
3. Откройте **"Functions"** или используйте **"Shell"** (если доступно)
4. Или локально выполните:

```bash
# Установите DATABASE_URL из Vercel
export DATABASE_URL="<скопируйте из Vercel Environment Variables>"
npx prisma db push
```

### Шаг 5: Перезапустите деплой

На Vercel нажмите **"Redeploy"** на последнем деплое.

---

## ✅ Решение 2: Использовать Railway (поддерживает SQLite лучше)

Railway лучше поддерживает SQLite и файловую систему.

### Шаг 1: Зайдите на Railway

1. Откройте https://railway.app
2. Войдите через GitHub

### Шаг 2: Создайте проект

1. **New Project** → **Deploy from GitHub repo**
2. Выберите ваш репозиторий

### Шаг 3: Настройте переменные

В разделе **Variables** добавьте:
```
DATABASE_URL=file:./dev.db
JWT_SECRET=ваш-секретный-ключ
NODE_ENV=production
```

### Шаг 4: Готово!

Railway автоматически задеплоит проект.

---

## ✅ Решение 3: Исправить DATABASE_URL для Vercel (временное)

Если хотите оставить SQLite, можно попробовать:

### В Environment Variables на Vercel:

```
DATABASE_URL=file:/tmp/dev.db
```

**НО:** Данные будут теряться при каждом деплое! Это не постоянное решение.

---

## 🎯 Рекомендация:

**Используйте Vercel Postgres** (Решение 1):
- ✅ Встроено в Vercel
- ✅ Бесплатно (до определенного лимита)
- ✅ Постоянное хранилище
- ✅ Автоматическая настройка
- ✅ Работает отлично с Next.js

---

## 📝 Быстрая инструкция для Vercel Postgres:

1. Измените `prisma/schema.prisma` на `provider = "postgresql"`
2. На Vercel: **Storage** → **Create Database** → **Postgres**
3. Vercel автоматически добавит `DATABASE_URL`
4. Локально выполните: `npx prisma db push` (с DATABASE_URL из Vercel)
5. Перезапустите деплой на Vercel

**Готово!**

