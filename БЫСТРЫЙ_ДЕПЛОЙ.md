# ⚡ Быстрый деплой на Render (пошагово)

## 🎯 Что нужно сделать:

### 1️⃣ Подготовка GitHub (5 минут)

```bash
# Если еще не инициализирован git
git init
git add .
git commit -m "Ready for Render deployment"

# Создайте репозиторий на GitHub и выполните:
git remote add origin https://github.com/ВАШ_USERNAME/messenger.git
git branch -M main
git push -u origin main
```

### 2️⃣ Создание базы данных на Render (2 минуты)

1. Зайдите на https://render.com
2. **New +** → **PostgreSQL**
3. Настройки:
   - Name: `messenger-db`
   - Plan: **Free**
   - Region: Frankfurt (или ближайший)
4. **Create Database**
5. ⚠️ **СКОПИРУЙТЕ Internal Database URL!** (он понадобится)

### 3️⃣ Деплой приложения (3 минуты)

1. **New +** → **Web Service**
2. Подключите ваш GitHub репозиторий
3. Настройки:
   - **Name**: `messenger` (или любое другое)
   - **Region**: Тот же, что база данных
   - **Branch**: `main`
   - **Build Command**: `npm install && npx prisma generate && npm run build`
   - **Start Command**: `npm start`
   - **Plan**: Free

4. **Environment Variables** (вкладка Environment):
   ```
   DATABASE_URL = <вставьте Internal Database URL>
   JWT_SECRET = my-super-secret-key-12345678901234567890
   NODE_ENV = production
   NEXT_PUBLIC_API_URL = https://messenger-xxxx.onrender.com
   ```
   ⚠️ `NEXT_PUBLIC_API_URL` заполните ПОСЛЕ деплоя (скопируйте URL сервиса)

5. **Create Web Service**

### 4️⃣ Применение миграций (1 минута)

После деплоя:

1. Откройте ваш Web Service
2. Вкладка **"Shell"**
3. Выполните:
   ```bash
   npx prisma db push
   ```

### 5️⃣ Готово! 🎉

Ваш мессенджер доступен по URL:
```
https://messenger-xxxx.onrender.com
```

**Отправьте этот URL друзьям!**

---

## 📝 Где найти SMS коды?

После деплоя коды будут в **Logs** вашего Web Service:
1. Откройте ваш Web Service на Render
2. Вкладка **"Logs"**
3. Найдите строку: `📱 SMS код для ...`

---

## ⚠️ Важно для Free плана:

- Сервис "засыпает" после 15 минут бездействия
- Первый запрос после пробуждения может занять 30-60 секунд
- Это нормально для бесплатного плана!

---

## 🔧 Если что-то не работает:

1. Проверьте **Logs** в Render
2. Убедитесь, что `DATABASE_URL` правильный (Internal URL!)
3. Проверьте, что миграции применены (`npx prisma db push`)
4. Убедитесь, что все переменные окружения установлены

---

## 💡 Совет:

После первого деплоя обновите `NEXT_PUBLIC_API_URL` в Environment Variables на реальный URL вашего сервиса, затем перезапустите (Manual Deploy).

