# 🚀 Автоматический деплой на Vercel

## ✅ Хорошие новости!

Vercel **автоматически** деплоит ваш проект при каждом обновлении на GitHub!

Вам нужно только:
1. Настроить Git один раз
2. При обновлении проекта - просто делать `git push`

---

## 📋 Шаг 1: Настройка Git (один раз)

### 1.1. Инициализируйте Git (если еще не сделано)

```bash
git init
```

### 1.2. Создайте репозиторий на GitHub

1. Зайдите на https://github.com/new
2. Создайте новый репозиторий (например: `messenger`)
3. **НЕ** добавляйте README, .gitignore, лицензию (у вас уже есть)
4. Нажмите **"Create repository"**

### 1.3. Подключите локальный проект к GitHub

```bash
git remote add origin https://github.com/ВАШ_USERNAME/messenger.git
```

(Замените `ВАШ_USERNAME` на ваш GitHub username)

### 1.4. Добавьте все файлы и загрузите

```bash
git add .
git commit -m "Initial commit"
git branch -M main
git push -u origin main
```

---

## 📋 Шаг 2: Подключение Vercel к GitHub (один раз)

### 2.1. Зайдите на Vercel

1. Откройте https://vercel.com
2. Войдите через **GitHub** (важно!)

### 2.2. Создайте проект

1. Нажмите **"Add New Project"**
2. Выберите ваш репозиторий `messenger`
3. Нажмите **"Import"**

### 2.3. Настройте Environment Variables

В разделе **"Environment Variables"** добавьте:

```
DATABASE_URL = file:./dev.db
JWT_SECRET = ваш-секретный-ключ-минимум-32-символа
NODE_ENV = production
```

### 2.4. Деплой

Нажмите **"Deploy"** - первый деплой займет 2-3 минуты.

---

## ✅ Готово! Теперь все автоматически!

После этого каждый раз, когда вы делаете `git push`, Vercel **автоматически** задеплоит новую версию!

---

## 🔄 Как обновлять проект (простой workflow)

### Когда вы внесли изменения в проект:

```bash
# 1. Добавить все изменения
git add .

# 2. Создать коммит с описанием
git commit -m "Обновил интерфейс чата"

# 3. Загрузить на GitHub
git push
```

**ВСЕ!** Vercel автоматически начнет деплой!

---

## 📱 Где посмотреть статус деплоя?

1. Зайдите на https://vercel.com
2. Откройте ваш проект `messenger`
3. Вкладка **"Deployments"** - там вы увидите все деплои

Статусы:
- 🟡 **Building** - идет сборка
- 🟢 **Ready** - готово, обновлено!
- 🔴 **Error** - ошибка (посмотрите логи)

---

## ⚡ Быстрые команды для обновления

Создайте файл `update.bat` (для Windows) в корне проекта:

```batch
@echo off
echo Добавляю изменения...
git add .
echo.
echo Введите описание изменений:
set /p message="> "
echo.
echo Создаю коммит...
git commit -m "%message%"
echo.
echo Загружаю на GitHub...
git push
echo.
echo Готово! Vercel автоматически задеплоит новую версию!
pause
```

Теперь просто запускайте `update.bat` и вводите описание изменений!

---

## 🔧 Полезные команды Git

### Посмотреть статус изменений:
```bash
git status
```

### Посмотреть что изменилось:
```bash
git diff
```

### Отменить изменения (если что-то пошло не так):
```bash
git restore .
```

### Посмотреть историю коммитов:
```bash
git log
```

---

## ⚠️ Важно:

1. **Всегда делайте `git add .`** перед коммитом
2. **Пишите понятные описания** в коммитах
3. **Проверяйте статус деплоя** на Vercel после `git push`

---

## 🎯 Примеры описаний коммитов:

- `"Исправил баг с авторизацией"`
- `"Добавил поиск пользователей"`
- `"Улучшил дизайн чата"`
- `"Обновил зависимости"`

---

## ✅ Итого:

1. ✅ Настройте Git и GitHub (один раз)
2. ✅ Подключите Vercel к GitHub (один раз)
3. ✅ При обновлении: `git add .` → `git commit -m "описание"` → `git push`
4. ✅ Vercel автоматически задеплоит!

**Просто и автоматически!** 🎉

