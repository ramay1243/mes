# ⚡ Быстрый деплой на Vercel (5 минут)

## 🎯 Vercel - самый простой вариант для Next.js!

### Шаг 1: Загрузите код на GitHub (если еще не загружен)

```bash
git init
git add .
git commit -m "Ready for Vercel"
git remote add origin https://github.com/ВАШ_USERNAME/messenger.git
git branch -M main
git push -u origin main
```

### Шаг 2: Зайдите на Vercel

1. Откройте https://vercel.com
2. Нажмите **"Sign Up"** или **"Log In"**
3. Войдите через **GitHub** (самый простой способ)

### Шаг 3: Создайте проект

1. Нажмите **"Add New Project"** (или **"New Project"**)
2. Выберите ваш репозиторий `messenger`
3. Нажмите **"Import"**

### Шаг 4: Настройте проект

Vercel автоматически определит Next.js! Просто проверьте:

- **Framework Preset**: Next.js ✅
- **Root Directory**: `./` ✅
- **Build Command**: `npm run build` ✅
- **Output Directory**: `.next` ✅

### Шаг 5: Добавьте Environment Variables

Нажмите **"Environment Variables"** и добавьте:

```
DATABASE_URL = file:./dev.db
JWT_SECRET = ваш-секретный-ключ-минимум-32-символа
NODE_ENV = production
```

**Важно:** 
- `DATABASE_URL` должен быть `file:./dev.db` (для SQLite)
- `JWT_SECRET` - придумайте сложную строку

### Шаг 6: Деплой!

1. Нажмите **"Deploy"**
2. Подождите 2-3 минуты
3. Готово! 🎉

### Шаг 7: Получите URL

После деплоя вы увидите:
```
✅ Production: https://messenger-xxxx.vercel.app
```

**Отправьте этот URL друзьям!**

---

## 📱 Где найти SMS коды?

### Вариант 1: Через Vercel Dashboard

1. Откройте ваш проект на Vercel
2. Вкладка **"Functions"** или **"Logs"**
3. Найдите строку: `📱 SMS код для ...`

### Вариант 2: Через Vercel CLI

```bash
npm install -g vercel
vercel login
vercel logs
```

---

## ⚠️ Важно про SQLite на Vercel:

- SQLite работает, но данные могут теряться при перезапуске
- Для тестирования - нормально
- Каждый деплой создает новую базу данных

---

## 🔄 Обновление кода:

После изменений просто:
```bash
git add .
git commit -m "Update"
git push
```

Vercel автоматически задеплоит новую версию!

---

## ✅ Готово!

Теперь ваш мессенджер доступен по постоянному URL, и друзья могут зайти в любое время!

**Преимущества Vercel:**
- ✅ Бесплатно
- ✅ Автоматический деплой
- ✅ HTTPS из коробки
- ✅ Быстро работает
- ✅ Не нужно настраивать базу данных

