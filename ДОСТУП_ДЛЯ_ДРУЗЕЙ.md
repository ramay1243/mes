# 🌐 Как дать доступ друзьям для тестирования

## Вариант 1: Через локальную сеть (если друзья в одной Wi-Fi сети) ⚡ БЫСТРО

### Шаг 1: Узнайте ваш IP адрес

**Windows (PowerShell):**
```powershell
ipconfig
```
Найдите "IPv4 Address" (например: 192.168.1.100)

**Или через команду:**
```powershell
ipconfig | findstr IPv4
```

### Шаг 2: Запустите сервер с доступом из сети

Измените команду запуска в `package.json` или запустите напрямую:

```bash
npm run dev -- -H 0.0.0.0
```

Или создайте новый скрипт в `package.json`:
```json
"dev:network": "next dev -H 0.0.0.0"
```

### Шаг 3: Отключите брандмауэр Windows (временно!)

1. Откройте "Брандмауэр Защитника Windows"
2. Временно отключите для частной сети (только для тестирования!)
3. Или добавьте правило для порта 3001 (3000)

### Шаг 4: Друзья подключаются

Друзья должны открыть в браузере:
```
http://ВАШ_IP:3001
```
Например: `http://192.168.1.100:3001`

---

## Вариант 2: Через интернет (ngrok) 🌍 ЛУЧШИЙ ВАРИАНТ

### Шаг 1: Установите ngrok

1. Скачайте с https://ngrok.com/download
2. Или через npm: `npm install -g ngrok`

### Шаг 2: Запустите ваш сервер

```bash
npm run dev
```

### Шаг 3: В новом терминале запустите ngrok

```bash
ngrok http 3001
```

(Если сервер на порту 3000, используйте `ngrok http 3000`)

### Шаг 4: Скопируйте URL

Вы увидите что-то вроде:
```
Forwarding  https://abc123.ngrok.io -> http://localhost:3001
```

**Этот URL (https://abc123.ngrok.io) отправьте друзьям!**

### Важно:
- URL работает пока запущен ngrok
- Бесплатная версия дает случайный URL каждый раз
- Для постоянного URL нужна платная версия

---

## Вариант 3: Через localtunnel (альтернатива ngrok) 🚀

### Шаг 1: Установите localtunnel

```bash
npm install -g localtunnel
```

### Шаг 2: Запустите туннель

```bash
lt --port 3001
```

(Если сервер на порту 3000, используйте `lt --port 3000`)

### Шаг 3: Скопируйте URL

Вы получите URL вида:
```
https://random-name.loca.lt
```

**Отправьте этот URL друзьям!**

---

## ⚠️ ВАЖНО для всех вариантов:

1. **Сервер должен быть запущен** пока друзья тестируют
2. **Не закрывайте терминал** с сервером
3. **SMS коды** будут в вашей консоли (не у друзей!)
4. **База данных** общая для всех - все видят одни сообщения

---

## 🔒 Безопасность (для тестирования можно пропустить):

- Это только для тестирования!
- В продакшене используйте HTTPS
- Настройте CORS правильно
- Используйте переменные окружения для секретов

---

## 📱 Инструкция для друзей:

1. Откройте браузер
2. Перейдите по ссылке, которую вы дали
3. Введите номер телефона
4. Нажмите "Отправить код"
5. **Попросите вас сказать код** (он в вашей консоли!)
6. Введите код и войдите
7. Готово! Можно общаться

---

## 🎯 Рекомендация:

Для быстрого тестирования используйте **ngrok** (Вариант 2) - самый простой и надежный способ!

