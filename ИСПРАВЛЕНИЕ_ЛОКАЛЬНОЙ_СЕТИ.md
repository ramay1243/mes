# 🔧 Исправление проблемы с локальной сетью

## ❌ Проблема: http://192.168.0.110:3001 не доступно

## ✅ Решение по шагам:

### Шаг 1: Проверьте, что сервер запущен правильно

Убедитесь, что сервер запущен с доступом из сети:

```bash
npm run dev:network
```

Или:
```bash
npm run dev -- -H 0.0.0.0
```

Должно появиться:
```
✓ Ready on 0.0.0.0:3001
```

**НЕ** должно быть `localhost` или `127.0.0.1`!

---

### Шаг 2: Проверьте правильный IP адрес

Выполните в PowerShell:
```powershell
ipconfig | findstr IPv4
```

Убедитесь, что используете правильный IP (обычно начинается с `192.168.` или `10.`)

---

### Шаг 3: Откройте порт в брандмауэре Windows

#### Способ 1: Через PowerShell (быстро)

Откройте PowerShell **от имени администратора** и выполните:

```powershell
New-NetFirewallRule -DisplayName "Next.js Dev Server" -Direction Inbound -LocalPort 3001 -Protocol TCP -Action Allow
```

(Если сервер на порту 3000, замените `3001` на `3000`)

#### Способ 2: Через графический интерфейс

1. Откройте "Брандмауэр Защитника Windows"
2. Нажмите "Дополнительные параметры"
3. Выберите "Правила для входящих подключений"
4. Нажмите "Создать правило"
5. Выберите "Порт" → "Далее"
6. Выберите "TCP" и введите порт `3001` → "Далее"
7. Выберите "Разрешить подключение" → "Далее"
8. Отметьте все профили → "Далее"
9. Введите имя: "Next.js Dev Server" → "Готово"

---

### Шаг 4: Проверьте, что друзья в той же сети

- Убедитесь, что все подключены к одной Wi-Fi сети
- Проверьте, что это не гостевой Wi-Fi (может блокировать доступ)

---

### Шаг 5: Проверьте доступность

На компьютере, где запущен сервер, откройте в браузере:
```
http://localhost:3001
```

Если работает локально, но не работает по IP - проблема в брандмауэре или сети.

---

## ✅ Альтернатива: Использовать Cloudflare Tunnel

Если локальная сеть не работает, используйте Cloudflare Tunnel:

### Шаг 1: Скачайте cloudflared

Скачайте с: https://github.com/cloudflare/cloudflared/releases/latest

Скачайте `cloudflared-windows-amd64.exe`, переименуйте в `cloudflared.exe`

### Шаг 2: Запустите сервер

```bash
npm run dev
```

### Шаг 3: Запустите туннель

В новом терминале (в папке, где `cloudflared.exe`):

```powershell
.\cloudflared.exe tunnel --url http://localhost:3001
```

### Шаг 4: Скопируйте URL

Вы получите URL вида:
```
https://random-name.trycloudflare.com
```

**Отправьте друзьям - работает из любой сети!**

---

## 🎯 Рекомендация:

**Если локальная сеть не работает:**
- Используйте **Cloudflare Tunnel** - работает из любой сети
- Или используйте **Vercel** для постоянного деплоя

---

## 📝 Быстрая проверка:

1. ✅ Сервер запущен с `-H 0.0.0.0`?
2. ✅ Порт открыт в брандмауэре?
3. ✅ Правильный IP адрес?
4. ✅ Друзья в той же сети?

Если все да, но не работает - используйте Cloudflare Tunnel!

