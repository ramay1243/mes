# 🔧 Исправление ошибки DATABASE_URL на Render

## ❌ Ошибка:
```
Environment variable not found: DATABASE_URL
```

## ✅ Решение:

### Шаг 1: Проверьте Environment Variables

1. Зайдите на https://dashboard.render.com
2. Откройте ваш **Web Service** (не базу данных!)
3. Перейдите во вкладку **"Environment"**
4. Проверьте, есть ли переменная `DATABASE_URL`

### Шаг 2: Добавьте DATABASE_URL (если её нет)

1. В разделе **"Environment Variables"** нажмите **"Add Environment Variable"**
2. Добавьте:
   - **Key**: `DATABASE_URL`
   - **Value**: Скопируйте **Internal Database URL** из вашей PostgreSQL базы данных
   
   ⚠️ **ВАЖНО:** Используйте **Internal Database URL**, а не External!
   
   Internal URL выглядит так:
   ```
   postgresql://messenger:password@dpg-xxxxx-a/messenger
   ```
   
   External URL выглядит так:
   ```
   postgresql://messenger:password@dpg-xxxxx-a.frankfurt-postgres.render.com/messenger
   ```

### Шаг 3: Где найти Internal Database URL?

1. Откройте вашу **PostgreSQL базу данных** на Render
2. В разделе **"Connections"** найдите **"Internal Database URL"**
3. Скопируйте его (он начинается с `postgresql://`)

### Шаг 4: Перезапустите сервис

После добавления/изменения переменной:

1. Перейдите в ваш Web Service
2. Нажмите **"Manual Deploy"** → **"Deploy latest commit"**
3. Или просто нажмите **"Restart"** (если есть такая кнопка)

### Шаг 5: Проверьте все переменные

Убедитесь, что у вас есть ВСЕ эти переменные:

```
DATABASE_URL = postgresql://... (Internal URL)
JWT_SECRET = ваш-секретный-ключ-минимум-32-символа
NODE_ENV = production
NEXT_PUBLIC_API_URL = https://ваш-сервис.onrender.com
```

---

## 🔍 Проверка через Shell

Можете проверить переменные через Shell:

1. Откройте ваш Web Service
2. Вкладка **"Shell"**
2. Выполните:
   ```bash
   echo $DATABASE_URL
   ```
   
   Если выводит пустоту - переменная не установлена!

---

## ⚠️ Частые ошибки:

### Ошибка 1: Использован External URL вместо Internal
**Решение:** Используйте Internal Database URL

### Ошибка 2: Переменная добавлена в базу данных, а не в Web Service
**Решение:** Переменные должны быть в **Web Service**, а не в базе данных!

### Ошибка 3: Не перезапустили сервис после добавления
**Решение:** Обязательно перезапустите сервис (Manual Deploy)

### Ошибка 4: Неправильный формат URL
**Решение:** URL должен начинаться с `postgresql://` и содержать все части

---

## 📝 Пример правильной настройки:

В разделе Environment Variables вашего Web Service должно быть:

```
DATABASE_URL = postgresql://messenger:abc123@dpg-xxxxx-a.frankfurt-postgres.render.com/messenger
JWT_SECRET = my-super-secret-jwt-key-12345678901234567890
NODE_ENV = production
NEXT_PUBLIC_API_URL = https://messenger-xxxx.onrender.com
```

---

## ✅ После исправления:

1. Перезапустите сервис
2. Подождите 1-2 минуты
3. Попробуйте снова отправить код
4. Проверьте логи - ошибка должна исчезнуть

