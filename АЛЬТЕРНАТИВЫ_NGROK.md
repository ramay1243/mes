# 🔄 Альтернативы ngrok (если сайт не работает)

## ✅ Вариант 1: Cloudflare Tunnel (БЕСПЛАТНО, БЕЗ РЕГИСТРАЦИИ!)

Cloudflare Tunnel - отличная альтернатива, работает без регистрации!

### Шаг 1: Установите cloudflared

Скачайте с: https://github.com/cloudflare/cloudflared/releases

Или через Chocolatey (если установлен):
```powershell
choco install cloudflared
```

Или через Scoop:
```powershell
scoop install cloudflared
```

### Шаг 2: Запустите туннель

Убедитесь, что сервер запущен (`npm run dev`), затем:

```powershell
cloudflared tunnel --url http://localhost:3001
```

(Если сервер на порту 3000, используйте `http://localhost:3000`)

### Шаг 3: Скопируйте URL

Вы получите URL вида:
```
https://random-name.trycloudflare.com
```

**Отправьте друзьям - работает сразу!**

---

## ✅ Вариант 2: Serveo (БЕЗ УСТАНОВКИ!)

Serveo работает через SSH, но можно использовать через curl!

### Шаг 1: Убедитесь, что сервер запущен

```bash
npm run dev
```

### Шаг 2: В новом терминале запустите туннель

```powershell
ssh -R 80:localhost:3001 serveo.net
```

(Если сервер на порту 3000, используйте `localhost:3000`)

**Примечание:** Может потребоваться установить OpenSSH или использовать Git Bash.

### Шаг 3: Скопируйте URL

Вы получите URL вида:
```
https://random-name.serveo.net
```

---

## ✅ Вариант 3: Локальная сеть (САМЫЙ ПРОСТОЙ!)

Если друзья в одной Wi-Fi сети - это самый простой вариант!

### Шаг 1: Узнайте ваш IP адрес

```powershell
ipconfig | findstr IPv4
```

Например: `192.168.0.110`

### Шаг 2: Запустите сервер с доступом из сети

```bash
npm run dev:network
```

Или:
```bash
npm run dev -- -H 0.0.0.0
```

### Шаг 3: Отключите брандмауэр (временно!)

1. Откройте "Брандмауэр Защитника Windows"
2. Временно отключите для частной сети
3. Или добавьте правило для порта 3001

### Шаг 4: Друзья подключаются

Друзья открывают в браузере:
```
http://192.168.0.110:3001
```

(Замените на ваш IP адрес)

**Работает без туннелей и паролей!**

---

## ✅ Вариант 4: Попробовать ngrok позже

Возможно, сайт временно недоступен. Попробуйте:
- Через некоторое время
- Через VPN
- Через другой браузер
- Проверьте интернет-соединение

---

## ✅ Вариант 5: Использовать Vercel (постоянный деплой)

Если нужно постоянное решение без туннелей:

1. Загрузите код на GitHub
2. Зайдите на https://vercel.com
3. Подключите репозиторий
4. Деплой займет 2-3 минуты
5. Получите постоянный URL

Подробнее в файле `БЫСТРЫЙ_ДЕПЛОЙ_VERCEL.md`

---

## 🎯 Рекомендация:

**Для быстрого тестирования:**
- **Cloudflare Tunnel** (Вариант 1) - работает без регистрации!
- **Локальная сеть** (Вариант 3) - если друзья рядом

**Для постоянного использования:**
- **Vercel** (Вариант 5) - постоянный URL, без туннелей

---

## 📝 Быстрая инструкция для Cloudflare Tunnel:

1. Скачайте: https://github.com/cloudflare/cloudflared/releases
2. Распакуйте `cloudflared.exe` в папку
3. Запустите сервер: `npm run dev`
4. Запустите туннель: `cloudflared tunnel --url http://localhost:3001`
5. Скопируйте URL и отправьте друзьям!

**Готово! Работает без регистрации!**

