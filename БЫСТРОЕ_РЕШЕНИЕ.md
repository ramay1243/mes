# ⚡ БЫСТРОЕ РЕШЕНИЕ - Cloudflare Tunnel

## Самый простой способ (2 минуты!)

### Шаг 1: Скачайте cloudflared

1. Зайдите на: https://github.com/cloudflare/cloudflared/releases/latest
2. Скачайте файл: `cloudflared-windows-amd64.exe`
3. Переименуйте его в `cloudflared.exe`
4. Поместите в папку (например, `C:\cloudflared\`)

### Шаг 2: Запустите ваш сервер

В терминале (где ваш проект):
```bash
npm run dev
```

Дождитесь сообщения:
```
✓ Ready on http://localhost:3001
```
(или 3000, если порт свободен)

**НЕ ЗАКРЫВАЙТЕ ЭТОТ ТЕРМИНАЛ!**

### Шаг 3: Запустите Cloudflare Tunnel

Откройте **НОВЫЙ** терминал (PowerShell) и выполните:

```powershell
cd C:\cloudflared
.\cloudflared.exe tunnel --url http://localhost:3001
```

(Если сервер на порту 3000, используйте `http://localhost:3000`)

### Шаг 4: Скопируйте URL

Вы увидите что-то вроде:
```
+--------------------------------------------------------------------------------------------+
|  Your quick Tunnel has been created! Visit it at (it may take some time to be reachable): |
|  https://random-name.trycloudflare.com                                                     |
+--------------------------------------------------------------------------------------------+
```

**Скопируйте URL `https://random-name.trycloudflare.com` и отправьте друзьям!**

### Готово! 🎉

Друзья могут открыть этот URL в браузере и пользоваться мессенджером!

**Работает из любой сети, без настройки брандмауэра, без паролей!**

---

## ⚠️ Важно:

1. **Сервер должен быть запущен** (`npm run dev`)
2. **Cloudflare Tunnel должен быть запущен** (второй терминал)
3. **Не закрывайте оба терминала!**

---

## 📱 Где найти SMS коды?

SMS коды будут в **первом терминале**, где запущен `npm run dev`.

Ищите строку:
```
📱 SMS код для 79991234567: 123456
```

---

## 🔄 Если Cloudflare Tunnel закрылся:

Просто запустите снова:
```powershell
cd C:\cloudflared
.\cloudflared.exe tunnel --url http://localhost:3001
```

URL может измениться - скопируйте новый и отправьте друзьям.

---

## ✅ Итого - самый простой путь:

1. `npm run dev` (запустить сервер)
2. `.\cloudflared.exe tunnel --url http://localhost:3001` (создать туннель)
3. Скопировать URL
4. Отправить друзьям
5. Готово!

**ВСЕ! Работает из любой сети!**

