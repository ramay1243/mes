# 🗄️ Настройка Neon PostgreSQL базы данных

## ✅ У вас есть строка подключения к Neon PostgreSQL!

Это отлично - Neon предоставляет бесплатную PostgreSQL базу данных.

## 📋 Шаг 1: Добавьте DATABASE_URL на Vercel

1. Зайдите на https://vercel.com
2. Откройте ваш проект `messenger`
3. Перейдите в **"Settings"** → **"Environment Variables"**
4. Найдите переменную `DATABASE_URL` (если есть) или нажмите **"Add New"**
5. Добавьте:
   - **Key**: `DATABASE_URL`
   - **Value**: `postgresql://neondb_owner:npg_T5HzWbc9tkev@ep-autumn-field-ahuje3qr-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require`
6. Выберите все окружения (Production, Preview, Development)
7. Нажмите **"Save"**

## 📋 Шаг 2: Примените миграции локально

### Вариант 1: Через временный .env.local

1. Создайте файл `.env.local` в корне проекта:
   ```
   DATABASE_URL=postgresql://neondb_owner:npg_T5HzWbc9tkev@ep-autumn-field-ahuje3qr-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require
   ```

2. Выполните:
   ```bash
   npx prisma generate
   npx prisma db push
   ```

3. **ВАЖНО:** Удалите `.env.local` после применения миграций (чтобы не закоммитить секреты!)

### Вариант 2: Через переменную окружения напрямую

В PowerShell:
```powershell
$env:DATABASE_URL="postgresql://neondb_owner:npg_T5HzWbc9tkev@ep-autumn-field-ahuje3qr-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require"
npx prisma generate
npx prisma db push
```

## 📋 Шаг 3: Обновите проект на GitHub

```bash
git add .
git commit -m "Настроил подключение к Neon PostgreSQL"
git push
```

## 📋 Шаг 4: Перезапустите деплой на Vercel

1. На Vercel откройте **"Deployments"**
2. Найдите последний деплой
3. Нажмите **"Redeploy"** (три точки → Redeploy)

## ✅ Готово! 🎉

После перезапуска все должно работать!

---

## ⚠️ ВАЖНО - Безопасность:

1. **НЕ коммитьте** `.env.local` с DATABASE_URL в Git!
2. **НЕ публикуйте** строку подключения в открытых местах!
3. Убедитесь, что `.env.local` в `.gitignore`

---

## 🔍 Проверка:

После деплоя:
1. Откройте ваш мессенджер
2. Попробуйте зарегистрироваться
3. Если работает - все настроено правильно!

---

## 📝 Если что-то не работает:

1. Проверьте **Logs** на Vercel
2. Убедитесь, что `DATABASE_URL` правильно добавлен в Environment Variables
3. Убедитесь, что миграции применены (`npx prisma db push`)
4. Проверьте, что схема Prisma использует `provider = "postgresql"`

