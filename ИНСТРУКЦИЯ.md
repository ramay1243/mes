# 📱 Подробная инструкция по запуску мессенджера

## Шаг 1: Проверка Node.js

Убедитесь, что у вас установлен Node.js версии 18 или выше.

**Проверка:**
```bash
node --version
npm --version
```

Если Node.js не установлен, скачайте с https://nodejs.org/

---

## Шаг 2: Установка зависимостей

Откройте терминал (PowerShell или Command Prompt) в папке проекта и выполните:

```bash
npm install
```

Эта команда установит все необходимые пакеты. Процесс может занять 1-3 минуты.

**Что происходит:** Устанавливаются Next.js, React, Prisma, TypeScript и другие зависимости.

---

## Шаг 3: Создание файла .env

Создайте файл `.env` в корне проекта (там же, где `package.json`).

**Способ 1 (через редактор):**
1. Создайте новый файл с именем `.env` (без расширения)
2. Скопируйте туда следующее содержимое:

```env
DATABASE_URL="file:./dev.db"
JWT_SECRET="my-super-secret-key-change-this-in-production-12345"
NEXT_PUBLIC_API_URL="http://localhost:3000"
```

**Способ 2 (через терминал):**
```bash
echo DATABASE_URL="file:./dev.db" > .env
echo JWT_SECRET="my-super-secret-key-change-this-in-production-12345" >> .env
echo NEXT_PUBLIC_API_URL="http://localhost:3000" >> .env
```

**Важно:** 
- Файл должен называться именно `.env` (с точкой в начале)
- Не добавляйте пробелы вокруг знака `=`
- В Windows может потребоваться создать файл через редактор, если терминал не работает

---

## Шаг 4: Генерация Prisma Client

Выполните команду для генерации клиента Prisma:

```bash
npm run db:generate
```

**Что происходит:** Prisma генерирует TypeScript типы для работы с базой данных.

**Ожидаемый результат:** Должно появиться сообщение `✔ Generated Prisma Client`

---

## Шаг 5: Создание базы данных

Выполните команду для создания структуры базы данных:

```bash
npm run db:push
```

**Что происходит:** Создается файл `dev.db` (SQLite база данных) с таблицами для пользователей, сообщений и кодов подтверждения.

**Ожидаемый результат:** Должно появиться сообщение об успешном применении схемы.

---

## Шаг 6: Запуск сервера разработки

Выполните команду:

```bash
npm run dev
```

**Что происходит:** 
- Запускается Next.js сервер разработки
- Компилируется TypeScript код
- Приложение становится доступным на http://localhost:3000

**Ожидаемый результат:** В терминале должно появиться:
```
  ▲ Next.js 14.x.x
  - Local:        http://localhost:3000
  - ready started server on 0.0.0.0:3000
```

**Важно:** Не закрывайте этот терминал! Сервер должен работать постоянно.

---

## Шаг 7: Открытие в браузере

1. Откройте браузер (Chrome, Firefox, Edge)
2. Перейдите по адресу: **http://localhost:3000**

Вы должны увидеть страницу авторизации с полем для ввода номера телефона.

---

## Шаг 8: Тестирование авторизации

### 8.1. Ввод номера телефона

1. Введите номер телефона (например: `+7 999 123 45 67` или `79991234567`)
2. Нажмите кнопку **"Отправить код"**

### 8.2. Получение кода

**ВАЖНО:** Код НЕ придет в браузер! 

Код будет выведен в **терминале**, где запущен `npm run dev`.

Найдите в терминале строку вида:
```
📱 SMS код для 79991234567: 123456
```

**Пример:**
```
> Ready on http://localhost:3000
📱 SMS код для 79991234567: 456789
```

### 8.3. Ввод кода

1. Скопируйте код из терминала (6 цифр)
2. Вставьте его в форму на странице
3. Нажмите **"Войти"**

### 8.4. Успешная авторизация

После ввода правильного кода вы попадете в интерфейс чата!

---

## 🎉 Готово!

Теперь вы можете:
- Видеть список всех пользователей в боковой панели
- Выбирать пользователя для личного чата
- Или использовать "Общий чат" для сообщений всем
- Отправлять и получать сообщения

---

## ❌ Решение проблем

### Проблема: "npm не является внутренней или внешней командой"

**Решение:** Node.js не установлен или не добавлен в PATH. Переустановите Node.js с https://nodejs.org/

---

### Проблема: "Port 3000 is already in use"

**Решение:** Порт 3000 уже занят другим приложением.

**Вариант 1:** Закройте другое приложение на порту 3000

**Вариант 2:** Запустите на другом порту:
```bash
npm run dev -- -p 3001
```
И откройте http://localhost:3001

---

### Проблема: "Error: Cannot find module"

**Решение:** Зависимости не установлены. Выполните:
```bash
npm install
```

---

### Проблем: "Prisma schema validation error"

**Решение:** Проверьте файл `prisma/schema.prisma`. Если он поврежден, восстановите из исходников проекта.

---

### Проблема: Код не появляется в терминале

**Решение:** 
1. Убедитесь, что сервер запущен (`npm run dev`)
2. Проверьте, что вы смотрите в правильный терминал
3. Попробуйте отправить код еще раз

---

### Проблема: "Database file not found"

**Решение:** База данных не создана. Выполните:
```bash
npm run db:push
```

---

## 📝 Полезные команды

- `npm run dev` - запуск сервера разработки
- `npm run build` - сборка для продакшена
- `npm run start` - запуск продакшен версии
- `npm run db:studio` - открыть Prisma Studio (GUI для базы данных)
- `npm run lint` - проверка кода на ошибки

---

## 🔧 Дополнительная настройка

### Изменение порта

В `package.json` измените скрипт:
```json
"dev": "next dev -p 3001"
```

### Использование PostgreSQL вместо SQLite

1. Измените `DATABASE_URL` в `.env`:
```env
DATABASE_URL="postgresql://user:password@localhost:5432/messenger"
```

2. В `prisma/schema.prisma` измените:
```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

3. Выполните:
```bash
npm run db:push
```

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте все шаги еще раз
2. Убедитесь, что все команды выполнены без ошибок
3. Проверьте, что файл `.env` создан правильно
4. Убедитесь, что порт 3000 свободен

