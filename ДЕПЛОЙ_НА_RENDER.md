# 🚀 Деплой мессенджера на Render.com

## Шаг 1: Подготовка базы данных

### 1.1. Измените схему Prisma для PostgreSQL

В файле `prisma/schema.prisma` измените:

```prisma
datasource db {
  provider = "postgresql"  // Было: sqlite
  url      = env("DATABASE_URL")
}
```

### 1.2. Создайте аккаунт на Render.com

1. Перейдите на https://render.com
2. Зарегистрируйтесь (можно через GitHub)
3. Подтвердите email

---

## Шаг 2: Создание базы данных PostgreSQL

1. В Dashboard Render нажмите **"New +"** → **"PostgreSQL"**
2. Настройки:
   - **Name**: `messenger-db`
   - **Database**: `messenger`
   - **User**: `messenger`
   - **Region**: Выберите ближайший (например, Frankfurt)
   - **Plan**: Free (для тестирования)
3. Нажмите **"Create Database"**
4. **ВАЖНО:** Скопируйте **Internal Database URL** (он понадобится позже!)

---

## Шаг 3: Подготовка репозитория GitHub

### 3.1. Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Создайте новый репозиторий (например: `messenger`)
3. **НЕ** добавляйте README, .gitignore, лицензию (у вас уже есть)

### 3.2. Загрузите код на GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/messenger.git
git push -u origin main
```

**Замените `ВАШ_USERNAME` на ваш GitHub username!**

---

## Шаг 4: Деплой приложения на Render

### 4.1. Создайте Web Service

1. В Dashboard Render нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий
3. Настройки:
   - **Name**: `messenger`
   - **Region**: Тот же, что и база данных
   - **Branch**: `main`
   - **Root Directory**: (оставьте пустым)
   - **Runtime**: `Node`
   - **Build Command**: `npm install && npx prisma generate && npm run build`
   - **Start Command**: `npm start`
   - **Plan**: Free

### 4.2. Настройте переменные окружения

В разделе **"Environment"** добавьте:

```
DATABASE_URL = <скопируйте Internal Database URL из базы данных>
JWT_SECRET = <любая случайная строка, минимум 32 символа>
NODE_ENV = production
NEXT_PUBLIC_API_URL = https://messenger.onrender.com
```

**ВАЖНО:** 
- `DATABASE_URL` должен быть **Internal Database URL** (начинается с `postgresql://`)
- `JWT_SECRET` - придумайте сложную строку (например: `my-super-secret-jwt-key-12345678901234567890`)
- `NEXT_PUBLIC_API_URL` - замените `messenger` на имя вашего сервиса

### 4.3. Запустите деплой

1. Нажмите **"Create Web Service"**
2. Дождитесь завершения деплоя (5-10 минут)
3. После деплоя нажмите **"Manual Deploy"** → **"Deploy latest commit"** (чтобы применить миграции)

---

## Шаг 5: Применение миграций базы данных

### 5.1. Через Render Shell

1. В вашем Web Service перейдите в **"Shell"**
2. Выполните:
```bash
npx prisma db push
```

### 5.2. Или через локальный терминал

1. Установите переменную окружения:
```bash
export DATABASE_URL="<ваш Internal Database URL>"
```

2. Выполните:
```bash
npx prisma db push
```

---

## Шаг 6: Получение URL

После успешного деплоя вы получите URL вида:
```
https://messenger-xxxx.onrender.com
```

**Этот URL отправьте друзьям!**

---

## ⚠️ Важные моменты

### Free план на Render:

- **Спиндаун:** Сервис засыпает после 15 минут бездействия
- **Первый запрос:** Может занять 30-60 секунд для "пробуждения"
- **Лимиты:** 750 часов в месяц бесплатно

### Для продакшена:

- Используйте **Starter** план ($7/месяц) - без спиндауна
- Настройте кастомный домен
- Используйте переменные окружения для секретов

---

## 🔧 Решение проблем

### Проблема: "Database connection failed"

**Решение:**
- Убедитесь, что используете **Internal Database URL** (не External!)
- Проверьте, что база данных создана и запущена
- Проверьте переменную `DATABASE_URL` в Environment

### Проблема: "Prisma Client not generated"

**Решение:**
- В Build Command должно быть: `npx prisma generate`
- Проверьте, что файл `prisma/schema.prisma` в репозитории

### Проблема: "Application error"

**Решение:**
- Проверьте логи в разделе **"Logs"**
- Убедитесь, что все переменные окружения установлены
- Проверьте, что миграции применены (`npx prisma db push`)

---

## 📱 Тестирование после деплоя

1. Откройте URL вашего приложения
2. Введите номер телефона
3. Нажмите "Отправить код"
4. **Код появится в логах Render!** (не в консоли)
   - Перейдите в ваш Web Service → **"Logs"**
   - Найдите строку: `📱 SMS код для ...`
5. Введите код
6. Готово!

---

## 🎉 Готово!

Теперь ваш мессенджер доступен по постоянному URL, и друзья могут зайти в любое время!

**Не забудьте:**
- На Free плане сервис может "засыпать" - первый запрос будет медленным
- Для постоянной работы используйте Starter план

