# 🗄️ Настройка Vercel Postgres (пошагово)

## Шаг 1: Измените схему Prisma

Файл `prisma/schema.prisma` уже изменен на PostgreSQL.

## Шаг 2: Создайте базу данных на Vercel

1. Зайдите на https://vercel.com
2. Откройте ваш проект `messenger`
3. В верхнем меню найдите **"Storage"** (или в боковом меню)
4. Нажмите **"Create Database"**
5. Выберите **"Postgres"**
6. Нажмите **"Create"**

## Шаг 3: Подключение автоматическое!

После создания базы:
- Vercel **автоматически** добавит `DATABASE_URL` в Environment Variables
- База будет подключена к вашему проекту

## Шаг 4: Примените миграции

### Вариант 1: Через Vercel Dashboard (если есть Shell)

1. Откройте ваш проект на Vercel
2. Найдите **"Shell"** или **"Functions"**
3. Выполните: `npx prisma db push`

### Вариант 2: Локально

1. На Vercel откройте **"Settings"** → **"Environment Variables"**
2. Скопируйте значение `DATABASE_URL`
3. Локально создайте временный `.env.local`:
   ```
   DATABASE_URL=<скопированное значение>
   ```
4. Выполните:
   ```bash
   npx prisma generate
   npx prisma db push
   ```
5. Удалите `.env.local` (чтобы не закоммитить)

## Шаг 5: Перезапустите деплой

1. На Vercel откройте **"Deployments"**
2. Найдите последний деплой
3. Нажмите **"Redeploy"** (три точки → Redeploy)

## Шаг 6: Готово! 🎉

После перезапуска все должно работать!

---

## ⚠️ Важно:

- **Не коммитьте** `.env.local` с DATABASE_URL!
- База данных на Vercel Postgres **бесплатна** до определенного лимита
- Данные теперь **постоянные** и не теряются при деплое

---

## 🔍 Проверка:

После деплоя попробуйте:
1. Открыть ваш мессенджер
2. Зарегистрироваться
3. Если работает - все настроено правильно!

---

## 📝 Если что-то не работает:

1. Проверьте **Logs** на Vercel
2. Убедитесь, что `DATABASE_URL` есть в Environment Variables
3. Убедитесь, что миграции применены (`npx prisma db push`)
4. Перезапустите деплой

