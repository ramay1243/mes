# 🚀 Простой вариант деплоя (без PostgreSQL)

## Вариант 1: Vercel (САМЫЙ ПРОСТОЙ) ⭐ РЕКОМЕНДУЮ

Vercel - это платформа от создателей Next.js, очень простая в использовании!

### Шаг 1: Установите Vercel CLI

```bash
npm install -g vercel
```

### Шаг 2: Загрузите код на GitHub

```bash
git init
git add .
git commit -m "Ready for Vercel"
git remote add origin https://github.com/ВАШ_USERNAME/messenger.git
git push -u origin main
```

### Шаг 3: Деплой через веб-интерфейс

1. Зайдите на https://vercel.com
2. Войдите через GitHub
3. Нажмите **"Add New Project"**
4. Выберите ваш репозиторий `messenger`
5. Настройки:
   - **Framework Preset**: Next.js (автоматически)
   - **Root Directory**: `./` (оставьте как есть)
   - **Environment Variables**: 
     - `DATABASE_URL`: `file:./dev.db` (для SQLite)
     - `JWT_SECRET`: `ваш-секретный-ключ`
     - `NEXT_PUBLIC_API_URL`: (оставьте пустым, будет автоматически)
6. Нажмите **"Deploy"**

### Шаг 4: Готово! 🎉

Через 2-3 минуты получите URL вида:
```
https://messenger-xxxx.vercel.app
```

**Отправьте этот URL друзьям!**

---

## Вариант 2: Railway (тоже простой)

Railway поддерживает SQLite и очень прост в настройке.

### Шаг 1: Зайдите на https://railway.app

### Шаг 2: Создайте проект

1. Войдите через GitHub
2. **New Project** → **Deploy from GitHub repo**
3. Выберите ваш репозиторий

### Шаг 3: Настройте переменные

В разделе **Variables** добавьте:
```
DATABASE_URL=file:./dev.db
JWT_SECRET=ваш-секретный-ключ
NODE_ENV=production
```

### Шаг 4: Готово!

Получите URL и отправьте друзьям.

---

## Вариант 3: Render с SQLite (не рекомендуется, но работает)

Если все-таки хотите использовать Render:

1. Создайте Web Service на Render
2. В Environment Variables добавьте:
   ```
   DATABASE_URL=file:./dev.db
   JWT_SECRET=ваш-секретный-ключ
   NODE_ENV=production
   ```
3. **ВАЖНО:** SQLite на Render не сохраняется между перезапусками на Free плане!

---

## ⚠️ Важно про SQLite на хостинге:

- SQLite работает, но данные могут теряться при перезапуске
- Для тестирования - нормально
- Для продакшена лучше PostgreSQL

---

## 🎯 Рекомендация:

**Используйте Vercel** - это самый простой вариант для Next.js:
- ✅ Бесплатно
- ✅ Автоматический деплой из GitHub
- ✅ HTTPS из коробки
- ✅ Быстро работает
- ✅ SQLite поддерживается

---

## 📝 После деплоя на Vercel:

1. Откройте ваш проект на Vercel
2. Перейдите в **Settings** → **Environment Variables**
3. Убедитесь, что `DATABASE_URL=file:./dev.db`
4. Перейдите в **Deployments**
5. Найдите последний деплой и откройте его
6. Скопируйте URL и отправьте друзьям!

---

## 🔍 Где найти SMS коды на Vercel?

1. Откройте ваш проект на Vercel
2. Вкладка **"Functions"** или **"Logs"**
3. Найдите строку: `📱 SMS код для ...`

Или используйте **Vercel CLI**:
```bash
vercel logs
```

